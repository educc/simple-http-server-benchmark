quarkus.http.port=3000

# Increase worker thread pool size for better concurrency
quarkus.thread-pool.core-threads=4
quarkus.thread-pool.max-threads=8

# Reactive route settings for better performance
quarkus.vertx.event-loops-pool-size=4

# Enable response compression
quarkus.http.enable-compression=true